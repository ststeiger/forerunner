param($installPath, $toolsPath, $package, $project)

#try
#{
#    $views = $project.ProjectItems.Item('Views')
#    $homeView = $views.ProjectItems.Item('Home')
#    $item = $homeView.ProjectItems.Item('Index.cshtml')
#}
#catch
#{
#}

#if ($item)
#{
#    throw "Error - This package will only work when installed into an Empty Web Application"
#}
